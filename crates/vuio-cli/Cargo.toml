[package]
name = "vuio-cli"
version = "0.0.45"
edition = "2021"
authors = ["vyrti"]
description = "VuIO media server command-line application"
license = "MIT OR Apache-2.0"
repository = "https://github.com/vuiodev/vuio"
homepage = "https://github.com/vuiodev/vuio"
readme = "../../README.md"
# Every binary is declared below. Without this, cargo also auto-discovers
# `src/bin/*.rs`, and an auto-discovered target carries no `required-features` —
# which is how `generate_test_media` came to be built in every configuration,
# including the ones without the `audiotags` dependency it needs.
autobins = false

[[bin]]
name = "vuio"
path = "src/main.rs"

[[bin]]
name = "generate_test_media"
path = "src/bin/generate_test_media.rs"
required-features = ["testdata"]

[features]
default = []
# The test-media generator needs a tag *writer*, which is the only thing
# audiotags is still here for. Gated so the shipped `vuio` binary does not link
# it.
testdata = ["dep:audiotags"]

[dependencies]
anyhow = "1.0"
audiotags = { version = "0.5", optional = true }
clap = { version = "4.6", features = ["derive"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls-no-provider"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.53", features = ["rt-multi-thread", "macros", "signal", "io-std", "io-util"] }
tracing = "0.1"
uuid = { version = "1.24", features = ["v4"] }
vuio-core = { path = "../vuio-core", version = "0.0.45" }

