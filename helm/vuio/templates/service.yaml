apiVersion: v1
kind: Service
metadata:
  name: {{ include "vuio.fullname" . }}
  labels:
    {{- include "vuio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.ssdpPort }}
      targetPort: ssdp
      protocol: UDP
      name: ssdp
  selector:
    {{- include "vuio.selectorLabels" . | nindent 4 }}
